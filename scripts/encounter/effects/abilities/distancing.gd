# @unimplemented
## Distancing — Encounter · origine vilgane · Toxic · coût — · dégâts —
##
## MÉCANIQUE (Notion) :
##   notes pour encyclopédie FR, inspi trad titre et Fx :
##   Simulation d'un désintérêt soudain tout en conservant une présence et une
##   communication bien que moindres. Méthode de pervers narcissique.
extends AbilityScript


func execute(ctx: EncounterContext) -> void:
	# TODO: implémenter la mécanique ci-dessus, puis retirer le « @unimplemented »
	# en tête. En attendant, les effets génériques dérivés des tags s'appliquent —
	# c'est un échafaudage, pas la mécanique réelle.
	super.execute(ctx)
