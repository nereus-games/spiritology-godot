# @unimplemented
## Distancing — Encounter · from vilgane · Toxic · cost — · damage —
##
## MECHANIC (from the design doc): the entry holds French notes for the encyclopaedia, kept as
## reference for translating the title and the effect. Feigning a sudden loss of interest while
## keeping up a presence and some communication, lessened; a narcissistic abuser's method.
extends AbilityScript


func execute(ctx: EncounterContext) -> void:
	# TODO: implement the mechanic above, then drop the leading "@unimplemented". Until then the
	# generic effects derived from the tags apply — that is scaffolding, not the real mechanic.
	super.execute(ctx)
